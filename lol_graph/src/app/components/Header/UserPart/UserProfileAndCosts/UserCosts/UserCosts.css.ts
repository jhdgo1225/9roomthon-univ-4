import { style } from "@vanilla-extract/css";

export const userCosts = style({
    display: "flex",
    flexDirection: "column",
    justifyContent: "center",
    padding: "0 10px",
    width: "min(115px, 100%)",
});
