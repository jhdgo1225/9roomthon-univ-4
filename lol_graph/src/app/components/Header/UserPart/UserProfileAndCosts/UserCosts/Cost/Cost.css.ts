import { style } from "@vanilla-extract/css";

export const cost = style({
    display: "flex",
    alignItems: "center",
    padding: "1px 2px 1px 10px",
});

export const costInfo = style({
    display: "flex",
    alignItems: "center",
    // padding: 3px 5px;
});

export const rp = style({
    border: "1px solid #c9b77c",
    borderRadius: "20px",
    padding: "1.5px 2px 1.5px 10px",
});
