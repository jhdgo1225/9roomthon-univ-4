import { lolBtn } from "./LOLBtn.css";

export default function LOLBtn() {
    return (
        <div>
            <button className={lolBtn}>LoR</button>
            <span>➚</span>
        </div>
    );
}
